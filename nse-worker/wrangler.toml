name = "nse-tracker"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "ebp-tracker-db"
database_id = "b93b206a-5537-4d12-8c86-a4b2372aae7f"

[vars]
ENVIRONMENT = "production"

# Secrets (set via `wrangler secret put`, not stored here):
#   CRON_SECRET       — guards POST /cron/nse
#   CLERK_SECRET_KEY  — guards the Clerk JWT routes (/nse/search,
#                        /user/nse-indicator-configs/*)
