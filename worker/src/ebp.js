// EBP detection stub
// An Engulfing Bar Print occurs when the current candle's body completely
// engulfs the previous candle's body in the opposite direction.

export function detectEBP(candles) {
  // Stub: returns { direction: 'bull' | 'bear' | null }
  return { direction: null }
}
