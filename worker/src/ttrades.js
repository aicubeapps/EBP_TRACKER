// TTrades bias stub
// Fetches higher timeframe trend bias from TTrades signals.

export async function getTTradesBias(symbol, timeframe, env) {
  // Stub: returns { bias: 'bull' | 'bear' | 'neutral' }
  return { bias: 'neutral' }
}
