name = "ebp-tracker-worker"
main = "src/ebp-worker.js"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "ebp-tracker-db"
database_id = "b93b206a-5537-4d12-8c86-a4b2372aae7f"

# EBP cron via cron-job.org HTTP triggers (POST /cron/ebp).
# Market breadth cron (was the CF native [triggers] cron here) has moved
# to compute-worker.